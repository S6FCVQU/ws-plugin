# Moved

This plugin has moved to **https://github.com/S6FCVQU/ws-plugin3**.

In Claude Code / Cowork:

```
/plugin marketplace add S6FCVQU/ws-plugin3
/plugin install geo-writers-skills@ws-plugin3
```

Remove the old marketplace and add the new one (Cowork freezes old records — a new repo is required).
